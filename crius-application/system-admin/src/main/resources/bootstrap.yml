server:
  port: ${SERVER_PORT:8011}
spring:
  application:
    name: system-admin
  cloud:
    nacos:
      discovery:
        server-addr: ${REGISTER_HOST:192.168.31.233}:${REGISTER_PORT:8848}
      config:
        server-addr: ${REGISTER_HOST:192.168.31.233}:${REGISTER_PORT:8848}
        file-extension: yml
management:
  endpoints:
    web:
      exposure:
        include: '*'