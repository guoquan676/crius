server:
  port: ${SERVER_PORT:8011}
spring:
  application:
    name: admin
  datasource:
    username: root
    password: 123456
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/crius?serverTimezone=UTC&useUnicode=true&useSSL=false&characterEncoding=utf8
# Logger Config
logging:
  level:
    com.baomidou.mybatisplus.samples.quickstart: debug